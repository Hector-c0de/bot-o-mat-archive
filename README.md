# bot-o-mat-archive
BOT-O-MAT is a Python desktop app for creating robots, assigning timed household tasks, and running those tasks in parallel. Users can manage multiple robots, monitor live progress, and see leaderboard rankings based on type-specific scoring rules.
